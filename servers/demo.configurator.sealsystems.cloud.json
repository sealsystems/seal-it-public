{
  "name": "example-server",
  "description": "An example MCP server for testing",
  "command": "node",
  "args": ["./dist/index.js"],
  "env": {
    "API_KEY": "${API_KEY}"
  },
  "requirements": {
    "node": ">=18.0.0"
  }
}
